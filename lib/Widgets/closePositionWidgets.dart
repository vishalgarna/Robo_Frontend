import 'package:flutter/material.dart';

class closePositionWidget extends StatelessWidget {
  const closePositionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No closePositions Are found'),
    );
  }
}
