import 'package:flutter/material.dart';

class pendingPositionWidget extends StatelessWidget {
  const pendingPositionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No pendingPositions Are found'),
    );
  }
}
