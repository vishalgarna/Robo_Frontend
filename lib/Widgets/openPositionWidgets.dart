import 'package:flutter/material.dart';

class openPositionWidget extends StatelessWidget {
  const openPositionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No openPositions Are found'),
    );
  }
}
